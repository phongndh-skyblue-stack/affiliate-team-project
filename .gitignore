# OS
.DS_Store
Thumbs.db

# Secrets / env (an toàn kép — không bao giờ commit file env thật)
*.env
server.env
client.env
!.env.example

# Dữ liệu nghiên cứu / artifact local (file lớn, không nên đưa lên git)
CMO Brain/
DA:BA Brain/
Gemini Artifacts/

# Log / kết quả chạy thử local
*.log
search_ads_result.json
